Features are somewhat localized.
(Sparse autoencoders, superposition, feature dictionaries.)

Neurons / directions can be semantically labeled.
(“This is a deception feature.”)

Causal intervention on small subspaces can isolate behavior.

Circuits are compositional.
(You can identify subgraphs that implement functions.)

Alignment-relevant behavior is at least partially localizable.