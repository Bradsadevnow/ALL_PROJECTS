When we design these chatbots, we need to define:

System prompt

Tone constraints

Behavioral constraints

Memory rules

Self-description rules

Response style envelope

But critically:

We should not change:

model weights

sampling config

decoding

prompt template format

Only identity layer changes.