Project Architecture
The Hardware: RADM MathBricc (Ryzen 780M + RTX 5060 Ti).

The Core Rig: monolith_circuits.py running a 48-hour automated sweep at 31.32 tok/s.

The Model: Gemma 3 4B-IT, currently materializing weights and running the 88-prompt suite.

The Discovery Tool: log_cat.py, designed to perform recursive, category-aware persistence analysis across the entire runs/ directory.

Established "Universal" Evidence
The Identity Backbone: Dimension 444 has been identified as a high-persistence anchor across layers 1 through 6.

The Metrics: Hit counts for 4:444 have exceeded 111,000 with a persistence ratio of ~0.72, holding steady across baseline, commitment, and transition prompts.

The Branching Point: Layer 6 shows the transition from the core backbone into specialized dimensions like 6:1981 and 6:1366.

The "Stateful System" Argument
The Hypothesis: Models are natively stateful; "steering vectors" and "truth circuits" are redundant interpretations of high-centrality coordinates that govern the system's dynamical trajectory regardless of task.

The Failure Metrics: Traditional interp expects different circuits for coding (H_skills) and recall (I_working_mem). Our sweep aims to prove they share the same structural backbone.

Pending Phases
Phase 2: Build three chatbots with distinct "identities" and re-trace the circuits to prove state-persistence.

Phase 3: Compile the final results into a "Stateful Identity" manifesto to challenge the current interp paradigm.